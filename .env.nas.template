# =============================================================================
# NAS Deployment Environment Variables
# Copy this to .env and fill in your values
# =============================================================================

# URLs for your domains (set these in Cloudflare Tunnel or reverse proxy)
NEXT_PUBLIC_BASE_URL=https://mail.affordablesolar.io

# PostgreSQL credentials
POSTGRES_USER=inbox
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=inboxzero

# Google OAuth credentials (from Google Cloud Console)
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_PUBSUB_TOPIC_NAME=projects/your-project/topics/inbox-zero

# Security secrets (generate with: openssl rand -hex 32)
AUTH_SECRET=
EMAIL_ENCRYPT_SECRET=
EMAIL_ENCRYPT_SALT=

# LLM Provider (choose one)
DEFAULT_LLM_PROVIDER=anthropic
DEFAULT_LLM_MODEL=claude-sonnet-4-5-20250929
ANTHROPIC_API_KEY=sk-ant-...

# Alternative: OpenRouter (access to multiple models)
# DEFAULT_LLM_PROVIDER=openrouter
# DEFAULT_LLM_MODEL=anthropic/claude-sonnet-4.5
# OPENROUTER_API_KEY=

# Alternative: OpenAI
# DEFAULT_LLM_PROVIDER=openai
# DEFAULT_LLM_MODEL=gpt-5.1
# OPENAI_API_KEY=

# =============================================================================
# Quick Setup Commands
# =============================================================================
# Generate secrets:
#   openssl rand -hex 32
#
# Start the stack:
#   docker compose -f docker-compose.nas.yml up -d
#
# View logs:
#   docker compose -f docker-compose.nas.yml logs -f
#
# Stop the stack:
#   docker compose -f docker-compose.nas.yml down
